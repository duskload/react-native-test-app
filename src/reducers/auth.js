
const initialState = {
  login: '',
  password: '',
  isLoggedIn: false,
  isLoading: false
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}